machine:
  php:
    version: 7.1.3
dependencies:
  pre:
    - pip install ansible
    - ansible-playbook -i /home/ubuntu/mycake3.app/ansible/hosts /home/ubuntu/mycake3.app/ansible/playbook.yml
  override:
    - composer update
